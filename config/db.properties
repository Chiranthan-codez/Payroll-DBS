# ─────────────────────────────────────────────────────────────────
# PayrollFX — Database Configuration
# Schema: payrolldbs_project.sql  (CSS 2212, MIT Manipal 2025-2026)
# ─────────────────────────────────────────────────────────────────

jdbc.driver=com.mysql.cj.jdbc.Driver
db.host=127.0.0.1
db.port=3307

# Database name — must match the CREATE DATABASE in payrolldbs_project.sql
db.name=payrolldbs_project

db.user=root
db.password=xzvb##1234A

# ─────────────────────────────────────────────────────────────────
# QUICK SETUP (run once)
# 1. Start MySQL (XAMPP → port 3306  |  WAMP → port 3306)
# 2. Import schema:
#       mysql -u root -p < database/payrolldbs_project.sql
# 3. Adjust db.user / db.password / db.port if different
# 4. Run application:
#       mvn javafx:run
#
# SEED LOGIN CREDENTIALS (plain-text passwords from SQL Section 3):
#   Admin    → username: aarav.hr        password: admin123
#   Employee → username: sneha.se        password: sneha123
#   Employee → username: rahul.jse       password: rahul123
#   Employee → username: priya.finance   password: priya123
#   Employee → username: arjun.marketing password: arjun123
#   HR Mgr   → username: anjali.hr       password: anjali123
#   Employee → username: vikram.finance  password: vikram123
#   Employee → username: deepa.marketing password: deepa123
#   Employee → username: karan.devops   password: karan123
#   Employee → username: swati.qa        password: swati123
#
# NEW IN payrolldbs_project.sql vs original payrolldbs.sql:
#   + employees.performance_rating  TINYINT DEFAULT 3  CHECK(1-5)
#   + employee_salaries.last_revised_date  DATE DEFAULT CURDATE()
#   + payroll_audit_log table (auto-written by triggers)
#   + 5 Stored Procedures: sp_get_employee, sp_add_employee,
#       sp_dept_salary_stats, sp_apply_salary_hike, sp_list_dept_employees
#   + 4 Functions: fn_get_net_pay, fn_dept_emp_count,
#       fn_years_of_service, fn_get_leave_balance
#   + 5 Triggers: trg_emp_before_insert, trg_salary_after_insert,
#       trg_salary_after_update, trg_emp_before_delete,
#       trg_user_after_insert
#   + 5 Views: vw_employee_full, vw_payslip, vw_leave_summary,
#       vw_user_auth, vw_dept_salary_stats
# ─────────────────────────────────────────────────────────────────
